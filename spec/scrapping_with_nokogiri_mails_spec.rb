require_relative '../lib/scrapping_with_nokogiri_mails.rb'

describe "what does the test assesses" do
  it "does thing x" do
    expect(a).to eq(b)
  end
  it "does thing y" do
    expect(b).to eq(d)
  end
end
